 We have reordered a given list of data by drag and drop. How will you achieve this problem statement?

var List=[item1,item2,item3,item4,item5]

Hint 1:- Use ListTitle and ReorderedListByDragDrop widget.

Hint 2:- Add logic for the reordered list by drag and drop. 

code:

import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    home: ReorderListExample(),
  ));
}

class ReorderListExample extends StatefulWidget {
  @override
  _ReorderListExampleState createState() => _ReorderListExampleState();
}

class _ReorderListExampleState extends State<ReorderListExample> {
  // Initial list
  final List<String> items = ['Item1', 'Item2', 'Item3', 'Item4', 'Item5'];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Reorder List by Drag & Drop'),
        centerTitle: true,
      ),
      body: ReorderableListView(
        padding: EdgeInsets.all(10),
        children: [
          for (final item in items)
            ListTile(
              key: ValueKey(item), // key is required for ReorderableListView
              title: Text(
                item,
                style: TextStyle(fontSize: 18),
              ),
              leading: Icon(Icons.drag_handle), // drag handle icon
            ),
        ],

        // Logic for reordering list items
        onReorder: (oldIndex, newIndex) {
          setState(() {
            // Adjust index when dragging downward
            if (newIndex > oldIndex) {
              newIndex -= 1;
            }

            // Move the item
            final element = items.removeAt(oldIndex);
            items.insert(newIndex, element);
          });
        },
      ),
    );
  }
}
