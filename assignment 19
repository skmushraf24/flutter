You have two buttons A & B . When you click on the A button the image will be shown and you click on the B button then your name, phone number, and email will be shown. How will you achieve this problem statement?

Hint 1:- Add the flag to show the different-2 data on click of buttons.

Hint 2:- Add text in the column for showing the name, phone number, and email.

Hint 3:- Add imageView to show the image.

Hint 4:- Add the image to the assets folder.

source code:
import 'package:flutter/material.dart';

class TwoButtonDisplay extends StatefulWidget {
  const TwoButtonDisplay({super.key});

  @override
  State<TwoButtonDisplay> createState() => _TwoButtonDisplayState();
}

class _TwoButtonDisplayState extends State<TwoButtonDisplay> {
  bool showImage = false; // Flag to toggle between image and text

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Two Button Example")),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // 🔹 Conditional Rendering Based on Flag
            showImage
                ? Image.asset(
                    "assets/profile.png",
                    width: 200,
                    height: 200,
                    fit: BoxFit.cover,
                  )
                : Column(
                    children: const [
                      Text(
                        "Name: John Doe",
                        style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
                      ),
                      Text(
                        "Phone: +91 9876543210",
                        style: TextStyle(fontSize: 16),
                      ),
                      Text(
                        "Email: johndoe@gmail.com",
                        style: TextStyle(fontSize: 16),
                      ),
                    ],
                  ),

            const SizedBox(height: 40),

            // 🔹 Buttons Row
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = true;
                    });
                  },
                  child: const Text("Button A (Show Image)"),
                ),
                const SizedBox(width: 20),
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = false;
                    });
                  },
                  child: const Text("Button B (Show Info)"),
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
